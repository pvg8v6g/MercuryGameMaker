<Application x:Class="GameMaker.AppMain.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:homeView="using:GameMaker.UX.Views.HomeView"
             xmlns:homeView1="using:GameMaker.UX.ViewModels.HomeView"
             xmlns:system="using:System">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
        <!-- Other merged dictionaries here -->
      </ResourceDictionary.MergedDictionaries>
      <!-- Other app resources here -->
      <DataTemplate x:Key="HomeTemplate" x:DataType="{system:Type homeView1:HomeViewViewModel}">
        <homeView:HomeView />
      </DataTemplate>
      <!-- <DataTemplate DataType="{system:Type TypeName=homeView1:HomeViewViewModel}"> -->
      <!--   <homeView:HomeView /> -->
      <!-- </DataTemplate> -->
    </ResourceDictionary>
  </Application.Resources>
</Application>
