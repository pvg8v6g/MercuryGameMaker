<UserControl x:Class="GameMaker.UX.Components.EngineListView.EngineReadOnlyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:componentModels="using:GameLibrary.Utilities.ComponentModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <ListView ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
            SelectedIndex="{x:Bind SelectedIndex, Mode=TwoWay}"
            SelectionMode="Single"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{StaticResource MediumGreyBrush}">
    <ListView.Resources>
      <DataTemplate x:Key="DefaultItemTemplate" x:DataType="componentModels:FileSelection">
        <TextBlock Text="{x:Bind FileName, Mode=OneWay}" />
      </DataTemplate>
    </ListView.Resources>
  </ListView>
</UserControl>
