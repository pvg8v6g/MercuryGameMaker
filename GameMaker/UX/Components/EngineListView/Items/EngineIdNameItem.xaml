<UserControl x:Class="GameMaker.UX.Components.EngineListView.Items.EngineIdNameItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="using:GameLibrary.Models"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="{x:Bind Model.Id, Mode=OneWay}" Grid.Column="0" />
        <TextBlock Text="{x:Bind Model.Name, Mode=OneWay}" Grid.Column="1" />
    </Grid>
</UserControl>
